metadata:
  labels:
    jenkins: jenkins-agent
spec:
  containers:
     - name: jnlp
       volumeMounts:
         - name: resolve-domain
           mountPath: /etc/hosts
  volumes:
     - name: resolve-domain
       hostPath:
         path: /etc/hosts   
